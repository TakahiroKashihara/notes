\documentclass[a4paper, amsfonts, amssymb, amsmath, reprint, showkeys, nofootinbib, twoside,superscriptaddress]{revtex4-1}
\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage{comment}
\usepackage[colorinlistoftodos, color=green!40,prependcaption]{todonotes}
\usepackage[pdftex, pdftitle={Article}, pdfauthor={Author}]{hyperref} % For hyperlinks in the PDF
%\setlength{\marginparwidth}{2.5cm}
\bibliographystyle{apsrev4-1}
\begin{document}
\title{Quantum metric in correlated systems}
\author{Takahiro Kashihara}
   % \email[Correspondence email address: ]{peters@scphys.kyoto-u.ac.jp}% Your name
\affiliation{Department of Physics, Kyoto University, Kyoto 606-8502, Japan}
\author{Michishita}
\affiliation{Japan}
\author{Robert Peters}
    \email[Correspondence email address: ]{peters@scphys.kyoto-u.ac.jp}% Your name
\affiliation{Department of Physics, Kyoto University, Kyoto 606-8502, Japan}
    \date{\today} % Leave empty to omit a date
\begin{abstract}
We show that
\end{abstract}
\keywords{}
\maketitle
\section{Introduction}
Why do people study quantum metric?
\section{Quantum metric in correlated systems}
\subsection{Definition}
We define Generalized Quantum Metric (GQM) by the following equation;

\begin{align}
g_{\alpha\beta}&=\mathrm{Re}\int d\omega_1 \frac{-1}{2\pi\omega_1^2}\int_{-\infty}^{\infty}\frac{d\omega}{2\pi}f(\omega)\\
\label{eg}
&\times \mathrm{Tr}[J_\alpha G^R(\omega+\omega_1)J_\beta (G^R-G^A) \\
&+ J_\alpha (G^R-G^A)J_\beta G^A(\omega-\omega_1)] + (\alpha\leftrightarrow\beta) 
\end{align}
Momentum dependece is ommited in the above equation.$f(\omega)$ is Fermi distribution function. $J_{\alpha},J_{\beta}$ is curent operator. $G^R$ and $G^A$ are retarded, advanced Grenn's function,respectively. In free systems, it coincides with the existing definition of Quantum Metric. This formula seems very complicated,  but we can relate it with optical conductivity. If we denote optical conductivity as
\begin{align}
    \sigma_{\alpha\beta}(\omega_1)=\displaystyle \sum_{k}\tilde{\sigma}_{\alpha\beta}(\omega_1,k) ,
\end{align}
then GQM is written as
\begin{align}
\label{defGQM}
g_{\alpha\beta}(k) = \mathrm{Re} \;\int_{0}^{\infty}d\omega_1\frac{\tilde{\sigma}_{\alpha\beta}(\omega_1,k) + \tilde{\sigma}_{\beta{\alpha}}(\omega_1,k)}{2\pi\omega_1}
\end{align}

This equation is valid not only in free systems but also in correlated systems if we ignore vertex corrections.

The important point is that GQM has semi positive definiteness(SPD).The detail of the proof is given in the next section.

So, GQM is real, symmetric (with respect to $\alpha$ and $\beta$) and has SPD. This is why we call GQM "Metric". 

\subsection{Proof }
We will show that GQM coincides with QM in free systems and that GQM has SPD.

First, let us check the former.

Linear optical conductivity can be written as
\begin{align}
\sigma_{\alpha\beta}(\omega_1) &=\displaystyle \sum_{k} (-\frac{1}{\omega_1})\int \frac{d\omega}{2\pi}f(\omega)\\
&\times \{\mathrm{Tr}[J_{\alpha\beta}(G^R-G^A)\\
&+J_\alpha G^R(\omega+\omega_1)J_\beta (G^R-G^A)\\
&+J_\alpha (G^R-G^A)J_\beta G^A(\omega-\omega_1)]\} .
\label{sigma}
\end{align}
The integrating function over BZ is $\tilde{\sigma}_{\alpha\beta}(\omega_1,k)$
The first term can be ignored because it is pure imaginary.

In calculating the second term, it is useful to take eigen vetors of $G^R$;
\begin{align}
(\mathrm{second\;term})
&= \mathrm{Tr}(J_\alpha G^R(\omega+\omega_1)J_\beta (G^R-G^A))\\
         &= (J_\alpha)_{nm}\frac{\delta_{ml}}{\omega + \omega_1 - E_m + i\delta}(J_\beta)_{lk}\\
         &\times (\frac{\delta_{kn}}{\omega - E_n + i\delta}-\frac{\delta_{kn}}{\omega - E_n - i\delta}))\\
         &=\frac{-2\pi i (J_\alpha)_{nm}(J_\beta)_{mn}}{\omega + \omega_1 -E_m + i\delta}\delta(\omega - E_n)
\end{eqnarray}
Then,
\begin{eqnarray}
\int \frac{d\omega}{2\pi}(\mathrm{second\; term}) = -i\frac{(J_\alpha)_{nm}(J_\beta)_{mn}}{\omega_1-(E_m-E_n)+i\delta}f(E_n)
\end{eqnarray}
Similary,
\begin{eqnarray}
(\mathrm{third\; term}) = \frac{-2\pi i (J_\alpha)_{nm}(J_\beta)_{mn}}{\omega - \omega_1 -E_n - i\delta}\delta(\omega - E_m)
\end{eqnarray}
\begin{eqnarray}
\int \frac{d\omega}{2\pi}(\mathrm{third\; term}) = i\frac{(J_\alpha)_{nm}(J_\beta)_{mn}}{\omega_1-(E_m-E_n)+i\delta}f(E_m)
\end{eqnarray}
Accordingly,we get
\begin{align}
\mathrm{Re}\quad \sigma_{\alpha\beta}(\omega_1) &= \frac{1}{\omega_1}\displaystyle \sum_k (f(E_n)-f(E_m))\\
&\times \mathrm{Re}\frac{i(J_\alpha)_{nm}(J_\beta)_{mn}}{\omega_1-(E_m-E_n) + i\delta}
\end{eqnarray}
Symmetrizing this formula,
\begin{align}
\mathrm{Re}\frac{\sigma_{\alpha\beta}(\omega_1) + \sigma_{\beta \alpha}(\omega_1)}{2} =\frac{1}{2\omega_1}\displaystyle \sum_k (f(E_n)-f(E_m))\\
\times \mathrm{Re}\frac{i((J_\alpha)_{nm}(J_\beta)_{mn}+(J_\beta )_{nm}(J_\alpha)_{mn})}{\omega_1-(E_m-E_n) + i\delta}
\end{align}
Now,note that
\begin{eqnarray}
(J_\alpha)_{nm}(J_\beta)_{mn}+(J_\beta )_{nm}(J_\alpha)_{mn}
\end{eqnarray}
is real. So,we get
\begin{align}
\mathrm{Re}\frac{i((J_\alpha)_{nm}(J_\beta)_{mn}+(J_\beta )_{nm}(J_\alpha)_{mn})}{\omega_1-(E_m-E_n) + i\delta} \\
= ((J_\alpha)_{nm}(J_\beta)_{mn}+(J_\beta )_{nm}(J_\alpha)_{mn})\\
\times \mathrm{Re}\frac{i}{\omega_1-(E_m-E_n) + i\delta}
\end{align}
Using
\begin{eqnarray}
\mathrm{Re}\frac{i}{\omega_1-(E_m-E_n) + i\delta}
=
\pi \delta(\omega_1-(E_m-E_n)),
\end{eqnarray}
we obtain
\begin{align}
\mathrm{Re}\frac{\sigma_{\alpha\beta}(\omega_1) + \sigma_{\beta \alpha}(\omega_1)}{2} =\frac{\pi}{2 \omega_1}\displaystyle \sum_k (f(E_n)-f(E_m))\\
\times ((J_\alpha)_{nm}(J_\beta)_{mn}+(J_\beta )_{nm}(J_\alpha)_{mn})\delta(\omega_1-(E_m-E_n))
\end{align}
When $T=0$, the integration function over BZ is
\begin{align}
\displaystyle \sum_{n:\mathrm{occupied}}\displaystyle \sum_{m:\mathrm{unoccpuied}}((J_\alpha)_{nm}(J_\beta)_{mn}+(J_\beta )_{nm}(J_\alpha)_{mn})\\
\times [\delta(\omega_1-(E_m-E_n))-\delta(\omega_1-(E_n-E_m))]
\end{align}
Then, we get
\begin{align}
g_{\alpha\beta}(k) = \mathrm{Re} \;\int_{0}^{\infty}d\omega_1\frac{\tilde{\sigma}_{\alpha\beta}(\omega_1,k) + \tilde{\sigma}_{\beta{\alpha}}(\omega_1,k)}{2\pi\omega_1}\\
= \frac{1}{2}\displaystyle \sum_{n:\mathrm{occupied}}\displaystyle \sum_{m:\mathrm{unoccpuied}} [\frac{\langle n|\partial_\alpha H |m\rangle\langle m|\partial_\beta H |n\rangle}{(E_n-E_m)^2} + (\alpha \leftrightarrow \beta)].
\end{align}
This is nothing but Quantum Metric defined in free systems.

Next, we show SPD of GQM.The strategy employed here is to represent the term by $G^R-G^A$(not $G^R$ only,nor $G^A$ only). This can be done by symmetrizing with respect to $\alpha$ and $\beta$. For example, after symmetrizing equation ($\ref{eg}$),
\begin{align}
\mathrm{Re} \; \mathrm{Tr}[J_\alpha G^R(\omega+\omega_1)J_\beta (G^R-G^A) +(\alpha \leftrightarrow \beta)]\\ 
=\mathrm{Re} \; \mathrm{Tr}[J_\alpha G^R(\omega+\omega_1)J_\beta (G^R-G^A) \\
+ J_\beta G^R(\omega+\omega_1)J_\alpha (G^R-G^A)]\\
= \mathrm{Re} \; \mathrm{Tr}[J_\alpha G^R(\omega+\omega_1)J_\beta (G^R-G^A) \\
+ \{J_\beta G^R(\omega+\omega_1)J_\alpha (G^R-G^A)\}^{\dagger}]\\
= \mathrm{Re} \; \mathrm{Tr}[J_\alpha G^R(\omega+\omega_1)J_\beta (G^R-G^A) \\
+(G^A-G^R)J_\alpha G^A(\omega + \omega_1)J_\beta]\\
= \mathrm{Re} \; \mathrm{Tr}[J_\alpha G^R(\omega+\omega_1)J_\beta (G^R-G^A)\\
+J_\alpha G^A(\omega+\omega_1)J_\beta(G^A-G^R)]\quad \\
\label{GRGA}
= \mathrm{Re} \; \mathrm{Tr}[J_\alpha (G^R(\omega+\omega_1)-G^A(\omega+\omega_1))J_\beta (G^R-G^A)]
\end{align}
Since $G^{A} = (G^{R})^{\dagger}$, $i(G^{R} - G^{A})$ is a Hermitian matrix. Thus, $i(G^{R} - G^{A})$ is diagonalizable by a unitary matrix, and the eigen values are real. We can diagonalize $G^{R} - G^{A}$ by using the same matrix with pure imaginary eigen values. We denote the eigen vetors as $|n\rangle$, and the corresponding eigen values as $-iA_n(A_n\in\mathbb{R})$.Note that $A_n \geq 0$ because it is spectral function.Then, eq.($\ref{GRGA}$) is equal to 
\begin{align}
-A_n(\omega)A_m(\omega+\omega_1)\{\langle n(\omega)|J_\alpha|m(\omega + \omega_1)\rangle + (\alpha\leftrightarrow\beta) \}/2.
\end{align}
In calculating the rest term, we utilize the same method. The difference is that we conduct a variable transformation
 \begin{align}
 \omega-\omega_1 \rightarrow \omega
 \end{align}
 Note that the argument of the Fermi distribution function also change as 
 \begin{align}
 f(\omega)\rightarrow f(\omega + \omega_1)
 \end{align}
 Finally, we obtain all the terms which contributes to GQM. Concretely,integrating function is can be written as
 \begin{align}
A_n(\omega)A_m(\omega+\omega_1)\{\langle n(\omega)|J_\alpha|m(\omega + \omega_1)\rangle\langle m(\omega+\omega_1)|J_\beta|n(\omega)\rangle \\
+ (\alpha\leftrightarrow\beta) \}\times \frac{f(\omega)-f(\omega+\omega_1)}{\omega_1^2}\times \frac{1}{8\pi^2}.
\end{align}
Let us introduce a function
\begin{align}
h(n,m,\omega,\omega_1) = A_n(\omega)A_m(\omega+\omega_1)\frac{f(\omega)-f(\omega+\omega_1)}{\omega_1^2}
\end{align}
The key point is that $h(n,m,\omega,\omega_1)$ is non negative because of the positivity of spectral function and the monotonicity of fermi distribution function.
Then, for arbitrary real numbers $c_\alpha$,
\begin{align}
\displaystyle \sum_{\alpha,\beta}c_\alpha g_{\alpha\beta}c_{\beta} &= \displaystyle \sum_{n,m,\alpha,\beta}\int_{0}^{\infty}d\omega_1\int_{-\infty}^{\infty}d\omega \;h(n,m,\omega,\omega_1)\;\\
&\times \langle n(\omega)|c_\alpha J_\alpha|m(\omega + \omega_1)\rangle \\
&\times \langle m(\omega+\omega_1)|c_\beta J_\beta|n(\omega)\rangle \times 2
\end{align}
Denoting 
\begin{align}
|\phi_n(\omega)\rangle = \displaystyle \sum_{\alpha} c_\alpha J_\alpha|n(\omega)\rangle ,
\end{align}
\begin{align}
\displaystyle \sum_{\alpha,\beta}c_\alpha g_{\alpha\beta}c_{\beta}=\displaystyle \sum_n \int_{0}^{\infty}d\omega_1\int_{-\infty}^{\infty}d\omega\\
\langle \phi_n(\omega)|\displaystyle \sum_m h(n,m,\omega,\omega_1)|m(\omega+\omega_1)\rangle\langle m(\omega+\omega_1)|\phi_n(\omega)\rangle \times 2 \\
\geq 0
\end{align}
This means GQM has SPD.
The inequality holds because
\begin{align}
Q(n,\omega,\omega_1) = \sum_m h(n,m,\omega,\omega_1)|m(\omega+\omega_1)\rangle\langle m(\omega+\omega_1)|
\end{align}
,which is superposition of projection operator has SPD;

\begin{align}
\langle \phi_n(\omega)|Q(n,\omega,\omega_1)|\phi_n(\omega)\rangle \geq 0
\end{align}
\section{numerical results}
\section*{Acknowledgements}
R.P. is supported by JSPS, KAKENHI Grant No. JP18K03511. 
The computation in this work has been done using the facilities of the Supercomputer Center, the Institute for Solid State Physics, the University of Tokyo.
%\bibliography{reference.bib}
\end{document}